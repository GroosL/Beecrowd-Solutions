#include <bits/stdc++.h>

int main() {
  int n, x;
  double y;
  std::cin >> n >> x >> y;
  std::cout << "NUMBER = " << n << '\n';
  std::cout << "SALARY = U$ " << std::fixed << std::setprecision(2) << x * y << '\n';
}
